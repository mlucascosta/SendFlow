<?php

declare(strict_types=1);

/**
 * Entrada legada do instalador (fora do public root).
 * Legacy installer entrypoint (outside public root).
 *
 * Mantida por compatibilidade durante a fase de transição.
 * Kept for compatibility during transition phase.
 */
echo 'Instalador SendFlow (em desenvolvimento).';
